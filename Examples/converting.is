str = "Hello";
val = 12;

def TryToConvert {
    if (OK(ConvertToInt($0))) {
        Write("The value was converted!");
    }
    else {
        Write(ERROR, RED);
        Exit(1);
    }
}

TryToConvert(val);
TryToConvert(str);
